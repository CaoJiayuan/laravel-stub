<?php namespace ${AppPath}\${AppName}\Repositories;

use Nerio\LaravelStub\Base\Repository as BaseRepository;
use ${AppPath}\${AppName}\Models\${AppNameS};

class ${AppName}Repository extends BaseRepository
{
    public function detail($id)
    {
        return ${AppNameS}::findOrFail($id);
    }
}
