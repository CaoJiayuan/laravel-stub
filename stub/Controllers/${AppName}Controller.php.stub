<?php namespace ${AppPath}\${AppName}\Controllers;

use Nerio\LaravelStub\Base\Controller;
use ${AppPath}\${AppName}\Repositories\${AppName}Repository;

/**
 * @author caojiayuan
 */
class ${AppName}Controller extends Controller
{
    public function detail(${AppName}Repository $repository, $id)
    {
        return $repository->detail($id);
    }
}
